include ../icon/icons

//- Модификаторы
//- ._active - Добавляет тень и меняет направление caret
//- ._size_small - уменьшенный селект

mixin select(items)
  +b.select&attributes(attributes)
    +e.SELECT.control
      each item in items
        option(value=item.value disabled=item.disabled selected=item.selected)= item.title
    +e.button
      +icon('icon-caret').caret
